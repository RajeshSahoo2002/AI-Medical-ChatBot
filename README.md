# AI-Medical-ChatBot

*Steps to run the AI Medical Project

-> Clone the repository

````bash
Project https://github.com/RajeshSahoo2002/AI-Medical-ChatBot
````

->Steps to create virtual env. using python command
   
   <!-- First check the python vertsion using command-->
   python3 --version

   <!--Create the vitual enviorment using below command-->
   python3 -m venv venv

   <!--Activate the virtual environment using below command-->
   source venv/bin/activate

   <!--Install the requirements.txt file using the below command-->
   pip install -r requirements.txt
